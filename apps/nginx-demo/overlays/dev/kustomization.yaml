apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-demo-dev

resources:
  - ../../base

labels:
  - pairs:
      environment: dev

replicas:
  - name: nginx-demo
    count: 1

patches:
  - path: patch-ingress.yaml
