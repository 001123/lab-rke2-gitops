apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: nginx-demo-prod

resources:
  - ../../base

labels:
  - pairs:
      environment: prod

replicas:
  - name: nginx-demo
    count: 3

patches:
  - path: patch-ingress.yaml
