apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

labels:
  - pairs:
      environment: prod

patches:
  - path: patch-infrastructure.yaml
  - path: patch-applicationset.yaml
